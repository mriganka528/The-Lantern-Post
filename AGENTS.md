# Lantern Post

Build the phase the user requests, using `Documentations/05_IMPLEMENTATION_PLAN.md` and the latest handoff for context.

The user is prioritising the complete in-app product and browser development. Android APK builds, EAS/Firebase configuration, and live device push setup are deferred until they explicitly return to that work. Preserve the prepared native setup without making it a prerequisite for building later product features.

On 14 September 2026 the user explicitly changed the release scope: the app must work WITHOUT automated moderation, which they will add in a later update after getting users. Default MODERATION_MODE=disabled permits real chat, friend letters and Infinity publication. Record these as unreviewed (moderationSkipped=true, moderationPassed=null, no moderationCheckedAt), never as a provider approval. Keep authentication, friendship/block checks, reports, limits, explicit confirmation and durable receipts. A future required mode must stop new sends if its provider is unavailable. Journey previews remain labelled, keep drafts intact and make no send request.

The user's enduring visual direction is **fairy tale, heavenly, vintage, and iconic**, including interactions such as opening palace doors. Carry this through every new screen, illustration, loading state, and transition. Read `Documentations/09_ART_DIRECTION.md` before visual work. Preserve the ivory paper, aged gold, soft celestial colours, storybook companions, and ornamental architecture.

Animations should be gentle, skippable when they interrupt navigation, and respect reduced motion. Keep Expo's managed workflow and bundle the curated artwork for reliable rendering.

Ambient motion defaults on and persists the user's manual switch across screens and reloads. Start animation clocks only after their visible world layers mount. Keep background/reduced-motion pauses without overwriting the stored choice. A finished burn must offer another letter; opening the desk after a saved terminal result starts a fresh page only after pending local cleanup is complete.

The user also requested a continuous arrival: after the gates open, the companion walks along the path to the palace. Keep the majestic background alive with a flowing river, waterfalls, queens on the terraces, and angels among the clouds. Preserve these additions in later phases.

Preserve the faster directional river and 850-ms waterfalls. The user removed all decorative birds from every palace and world for performance, including animated flocks and etched background birds. Do not restore them. The owl, swan and peacock remain selectable companions. The sealing court keeps gold columns, cloud temples, roses, lanterns, angels and fairy lights around a skippable fold/ribbon/wax ritual. Sealing alone never sends.

The writing desk and presets should feel like an old palace scriptorium: carved wood, weathered paper edges, engraved borders, royal crests, muted ribbons, and antique wax. Preserve readable ink above the decorative paper layers.

The Royal Collection adds four companions and six ornate presets. All active items are free now; preserve server-owned `collection: royal` metadata and stable IDs for later premium entitlements. Do not add payment/locking behaviour until requested. Keep the small Infinity star glyphs with large accessible touch targets and a single short interaction sparkle, not constant per-star animation loops.

The Burning World should feel like a majestic celestial fire kingdom, with floating temples, living flames, and a crowned fire guardian. Preserve zoom and pan, a slow progressive paper burn with visible charring, embers and ash, and time to explore the completed scene before returning. Keep the letter's ignition behind confirmed release and local cleanup; ambient realm flames can burn before confirmation.

The friendship court uses a royal guestbook, rose-covered sage gates, sealed invitation cards, and companions beside each friend's gate. Open a friend's gate only after confirmed acceptance. Keep recipient-only responses, mutual friendship checks, account isolation, and filtering of blocks in both directions when adding friend delivery.

Private letters retain their original antique stationery. The courier walks continuously to the selected friend's gate after confirmed delivery, and receiving opens a sealed envelope onto readable manuscript paper. Preserve durable delivery/cancellation receipts, pending-draft fences, current friendship/block checks, and the distinction between previews and real deliveries.

Safety controls are part of the product: blocks end friendship in both directions; unblocking never restores it automatically. Keep recipient-only deduplicated reports, optional atomic report-and-block, private closed-gate lists, persistent request budgets and durable daily invitation limits. Receipt lookup/cancellation must remain usable under send throttling. Reports are saved for eventual review without promising a live response. Hosted moderation, its workers and an administrator review screen are deferred to a future update; basic report submission and blocking stay available.

Backend configuration and private credentials belong in `server/.env`; client public settings belong in `client/.env`. Never put server credentials in client code or browser fixtures.

Voice letters use the antique gramophone composer and original stationery, with explicit microphone permission, a three-minute limit, local account-scoped bytes and no autoplay. Keep deletion queues durable across draft upgrades (v6 preserves versions 1–5). Optional written voice captions follow the current review mode (separate text approval only when automated moderation is enabled), remain out of lists, and are cleared with the recording. Burning uploads neither audio nor captions; confirmed release and local cleanup precede the burn. Uploads stay bound to owner/request/destination, with a recipient for private deliveries, and checked bytes are copied to a separate sealed object before delivery and any future enabled content review. Preserve current friendship/block checks and never label an unreviewed delivery as moderation-approved.

Infinity World letters are unsigned by default. Only explicit Sign it may reveal a username; public markers/readers must not embed account IDs, companions or private social cards. Public IDs are random and separate from operation receipts. Reporting/blocking unsigned letters uses opaque aliases and must not expose an author in closed-gate lists. Keep viewport reads/rendering bounded, sample letters clearly labelled and separate from live data, and previews free of sends/uploads/draft changes. Preserve the continuous infinity-gate walk and the confirmation/local-cleanup fence before real publication animation.

Foreground recovery may retry only a durably saved, already-confirmed pending operation, after receipt lookup and capability checks, using its original UUID. Stop further attempts when leaving/backgrounding or changing identity. Keep support suggestions local, dismissible, non-diagnostic and non-blocking. Diagnostics are off by default and consent-scoped; never collect letter/voice/caption content, names, arbitrary errors/stacks or support-match flags. Retention may clear due deleted content and old unreported audit stubs, never the operation receipts needed for recovery and limits.

The correspondence cabinet supports multiple independent letters per account/device. Keep the original single-draft key as a legacy entry and v1–v6 recovery intact; new UUID document keys avoid shared-index races. Pending operations fence only their own letter. Starting another page must preserve other words, stationery, recordings and UUIDs. Do not retain sent/burned contents in completed records. Inactive letters may run local recording cleanup, never automatic network retries; active-editor recovery remains receipt-first. Stop recording/saving before switching pages, and preserve unsaved text on storage errors.

Unfinished writing/sealed letters can be removed from My letters only after confirmation. Recheck the exact saved revision and stage at confirmation; never discard pending operations or completed receipts. Clear words, captions and recording metadata together, keeping only a content-free removed-letter marker with a durable audio deletion queue. Never reuse a removed document key or allow stale editors/reset actions to restore it. Removal must preserve other letters and accounts and leave the original intact if its storage write fails. Keep a visible retry for queued recording cleanup. Royal navigation uses bundled sage-and-gold engraving, crests, framed buttons and accessible hover/focus/press feedback without idle animation loops; keep one My palace action in the desk header and preserve local My letter/My sealed letter return controls.

Share a copy is explicit, author-initiated export of an owned writing/sealed letter. It does not send, upload, publish, sign or change the original. Browser exports preserve stationery in paginated PNGs and offer recording files; phone text uses the system share sheet. Never export pending or cleared letters, account/recipient IDs, receipt tokens or captions without an explicit words action. Expo Sharing, ViewShot and DocumentPicker are installed. Native stationery PNG pages and audio sharing are implemented; actual phone share-sheet/file-picker verification remains deferred. Do not call native sharing device-verified based on a JavaScript export alone. The selected desk companion writes only while typing, records only while the microphone is recording, and respects motion/foreground preferences.

Phase 11 policy drafts use The Lantern Post as the publisher name and a minimum age of 13. They are review drafts until the operator’s legal identity, country, public contact, provider/retention details and launch terms are supplied. Keep load rehearsals isolated with synthetic identities and test providers; real database load requires an explicitly named local test/CI database. Never run a load test against the live product or treat fixture timings as production capacity. Native internal testing, live services and publication remain deferred.

Home feature sections stay in this order below the palace arrival: writing desk, friendship gates, worlds, then companion/other details. The sealed-letter destination court uses three illustrated royal portals; selecting one still opens the established confirmation/preview flow. Username calling cards share only the public username and fixed invitation text, never account IDs, email, credentials or an unconfigured localhost link.

Friend chat uses authenticated, bounded WebSocket subscriptions with token refresh, cursor reconnect and heartbeat timeouts (HTTP polling remains a compatibility endpoint) with current accepted-friend and bidirectional-block checks. Automated moderation is disabled for this release; confirmed messages persist with explicit unreviewed metadata. Provider-based checks are deferred to a future update. Keep thread sequence allocation atomic with message commit, durable owner/request/peer-bound receipts, recipient-only deduplicated reports, independent receipt/cancellation budgets, and original-UUID recovery only for saved confirmed intents. Stop listeners/retries on route exit, backgrounding, account changes and sample preview. The sample parlour is labelled, uses scripted local replies, makes no send and preserves the real draft. Chat content and usernames never enter diagnostics. Account erasure clears readable chat content and keeps content-free receipts and report relations.

Account removal requires typed username and explicit confirmation. Close access and clear server content atomically, then retry Clerk, voice and connected Drive cleanup through the durable deletion outbox. Never recreate a closed account through onboarding. Local closure markers fence old editors and voice/export writes; cleanup is owner-scoped and retryable. Other devices and exported copies cannot be recalled.

Chat backups are manually created encrypted AES-256-GCM archives, with a generated recovery key shown only to the user. Keep keys off the server and Drive. Direct Drive OAuth uses one-use PKCE state, server-encrypted refresh tokens and drive.appdata only. Credentials belong only in server/.env. Local and Drive copies stay encrypted; restoration opens a read-only archive for the original account and currently open friendships, never replaying messages or pending operations. Account erasure waits for in-flight Drive exchanges/uploads before cleanup; external failures remain pending. Google OAuth configuration and real-account acceptance testing still require final operator setup.

The user requires a genuinely free service with no payment card. Supabase Free is the prepared voice-delivery option; do not enable a paid plan, card-required R2/AWS onboarding or billing upgrades. The operator must confirm Free/no-card signup and configure server-only Supabase settings. Existing database migrations are user-confirmed complete; this storage pass needs no migration.

Voice drafts stay local. On explicit friend/public delivery, Supabase receives a private shared copy through the authenticated API, never a browser service_role key or a long-lived upload token. Verify the bucket is private, retain hash/duration/owner/receipt checks, reserve both object copies against the configured free-storage cap, and retry cleanup durably. HTTP playback signing happens outside database transactions, with fresh access checks before exposing the link.

Optional voice keepsakes back up one owned writing/sealed recording per AES-GCM archive, locally or via the existing private Google Drive connection. Caption inclusion requires an explicit checkbox. Keys stay with the user; opening after reinstall verifies account ownership and permits playback/file saving without sending or replaying a letter. Recheck the source revision before backup saving, keep pending/completed/removed letters out of backup creation, and clear decrypted playback data on close/background/account changes.

Latest refinement: private-letter removal is per participant. senderDeletedAt and recipientDeletedAt hide only that side; clear shared words/captions/audio only when both sides have removed it. Keep receipts and original request UUIDs. Full account erasure remains a distinct global cleanup. Do not restore the old remove-from-both behavior for ordinary private-letter deletion.

Account-wide /events/socket updates drive friendship/letterbox refresh and content-free arrival alerts. Persist owner-scoped cursors; allocate sequences with the originating transaction; stop on background/identity changes; recheck visibility before alerts and deduplicate phone/in-app arrivals. Alerts default on with a persistent manual switch, stay quiet in the open matching chat, and wait for blocking dialogs to close. Chat cursors advance only on ordered server pages, never on an immediate own-message receipt.

The user is currently browser-only: enable in-app alerts now and prepare phone push for later. Android builds, EAS/Firebase credentials and physical-device notification verification remain deferred by their latest answer. Phone push includes chat messages and private letters, carries no content, and never bypasses friendship/block/account checks. Retain free/no-card setup choices.

The latest mobile refinement supersedes oversized royal action styling: links/buttons use minimal decoration, small optional icons, thin gold borders and at least 44-pixel touch targets. Keep the fairy-tale artwork and paper; do not restore large inset frames/medallions on controls. Stack the friendship heading above its action on phones, keep writing desk/friendship/worlds order, and use compact illustrated destination cards below 600 pixels. Dialogs/readers must leave enough width for words. Infinity opening has one bounded request per account/star/retry; same-account transport refresh must not restart it. API deadlines include authentication, fetch, body reading and the one token retry; late tokens after timeout/cancellation must never send.

Session tokens must stay stable for each Clerk client/session; Expo useAuth currently creates a new getToken wrapper on every render. Never use that wrapper as a live-subscription dependency. Share in-flight refreshes, use the SDK cache for socket connections, honor 429 cooldowns, and preserve before/after identity fences including queued forced refreshes. Do not restore unconditional skipCache on reconnect. Use style.pointerEvents and the Image tintColor prop to avoid browser deprecations.

The user requested a first-install welcome notification. Prepare it locally on the first foreground launch of an installed Android/iOS app, after OS permission; never claim notification delivery before the app opens. Keep a per-installation, content-free completion/permission-attempt marker, deterministic notification ID, duplicate checks and explicit settings retry. Browser/Expo Go must not consume the marker. Local welcome needs no server or paid service and shares the native foreground notification handler with account push. Physical device testing and remote-push setup remain deferred. Infinity actions may have light celestial flourishes and small icons while keeping compact 44-pixel targets.

Infinity World opens in night view on every fresh visit, as explicitly requested. Users may choose Day view or Night view during that visit; do not persist Day as the next visit default. Keep the compact 44-pixel switch, brighter night letter stars, bundled moonlit architecture, and original day art. Switching view must preserve the current camera and independent saved motion setting, and must never send, upload or mutate a draft. Journeys default to night.

The signed-in app has one compact vintage notification bell at the upper right of story and desk headers. Keep 44-pixel targets, the phone desk navigation on its own row, and no idle bell loop. Its panel shows the latest 50 currently accessible arrivals from seven days, including while the browser was closed. Use the existing account event socket and batched authenticated history reads, not a second socket. Bell history works with popups off. Seen IDs are account/device scoped, bounded and covered by account cleanup/fences; marking notices seen must never delete/read a letter or replay a send. Active matching-chat arrivals are already seen. Recheck blocks, account state, message erasure and each side of letter deletion; exclude private content and usernames from notification payloads. Bell navigation must honor recording and unsaved-draft guards.

The message parlour opens from one authenticated socket snapshot (after:null): latest 30 messages plus send availability, then the live stream. Keep earlier-page access, current account/friend/block checks and the shared durable history budget. Resume reconnects from the last ordered cursor; do not replace the transcript with a fresh tail or advance past unseen messages using own-send receipts. Pending receipt recovery must not block receiving messages, must be deduplicated per active generation, and stays original-UUID/receipt-first. A slow or unavailable socket can use cancelled-race HTTP history/poll fallback; terminal gate/session/throttle errors cannot be bypassed. Do not restore separate blocking history/capability startup calls or add persistent plaintext transcript caching for speed.

The account menu keeps Chat backups & privacy as a prominent compact sage button below username sharing. The user removed Find someone to talk to from this menu; do not restore that account-menu shortcut. Contextual writing/chat support is separate. Conditional notice strings in View/ScrollView content must use Boolean(notice) or a null-returning conditional so empty strings never become raw React Native text nodes. Account removal still requires exact typed username and explicit confirmation.

The user confirmed completing real-account testing for the current product. Record this as user-reported acceptance and do not keep listing that step as unfinished. See Documentations/37_RELEASE_STATUS.md for the current remaining-work checklist. Google Drive OAuth setup and separately deferred native/device-push/public-release work remain distinct; new changes can still require targeted regression testing.

Latest priority: the user explicitly resumed mobile work first, superseding older native-work deferrals. They now report completing the first Expo/project/development-APK setup instructions. The local Expo project ID is configured and the development build-input check passes; APK installation is user-reported, not independently device-verified. Native functional acceptance is next: see Documentations/39_ANDROID_DEVICE_CHECKLIST.md and the historical preparation in Documentations/38_ANDROID_READINESS_HANDOFF.md. Do not relist Expo project creation/first APK setup as unfinished unless a concrete failure requires it. Preserve optional Firebase for initial builds, the light-theme config plugin, private local draft storage, and the development/standalone profile distinction. Backend hosting and Google Drive configuration remain later tasks. The user excludes beta/publication steps from the development checklist.

Latest hosting choice: the user reported that Northflank requires a payment card and explicitly chose Render after discussing Free-plan idle sleep and usage limits. This supersedes the earlier exclusion of Render. Use the existing root Dockerfile/.dockerignore for one Render Free web service, repository root context, port 3000 and /health/ready. Add no new database, disk, worker or paid resource. Keep the no-card requirement; if Render also asks for a card, report it before proceeding. See Documentations/40_PUBLIC_API_DEPLOYMENT.md. Prisma generation/compilation make no DB changes and existing migrations remain user-confirmed complete. Set WEB_ORIGINS to the actual Render HTTPS origin (replace the Northflank origin) so Android WebSocket Origin checks pass. Verify HTTPS health and both WSS paths with check:hosted-api; it performs no real sends or subscriptions. Full Linux image build and the live Render URL remain unverified. Uptime monitoring was discussed but no monitor is configured; do not claim it guarantees continuous availability or removes free quotas. Do not add paid resources or replace WSS/background jobs with Vercel Functions without an explicit task. Keep the native API address unchanged until a real URL is available. Moving to hosting does not mark native device acceptance complete.

Latest connection update: the user reports deploying https://the-lantern-post.onrender.com and requested it as the default across the project, with local operation optional. Both platform env files/defaults now use Render. See Documentations/41_RENDER_DEFAULT_HANDOFF.md. Explicit api:local/api:hosted commands change public endpoint settings while preserving credentials and drafts; a local backend still must be started by the operator. Do not introduce automatic per-request host switching or replay sends against another server. DEVELOPMENT_WEB_ORIGINS explicitly opts trusted loopback/private IPv4 UI/socket origins into CORS, Clerk authorized parties and socket checks even on the hosted API; normal production WEB_ORIGINS still requires HTTPS. The new code and flag must be applied on Render for local browser development, and check:hosted-api --web-origin verifies that configuration without credentials or real sends. The legacy root .env is not used for client/server configuration. Hosting success is user-reported; outbound verification was blocked by network permissions, not evidence that Render was down.

Latest acceptance: the user confirms the Render/client connection now works after CORS setup. Do not keep listing Render deployment or browser-origin configuration as unfinished. Native phone acceptance, remote Android push, direct Drive setup, operational safeguards, final policy details and the standalone APK remain separate. No Firebase Android file or Drive configuration is present locally at this check. Private server credentials appeared in IDE selections; rotation was recommended but is not confirmed. See the updated priority list in Documentations/37_RELEASE_STATUS.md; no beta/publication tasks should be added to that development checklist.

Latest notification status: the user now reports the Firebase/Expo notification setup complete. The Firebase Android file is present and matches the app package, androidPushConfigured resolves true, and local EXPO_PUSH_ENABLED=true is verified; Render enablement is user-reported. Do not relist notification configuration as unfinished. A real phone delivery result is not independently verified; keep any remaining device checks distinct. Direct Drive settings are still absent locally. Documentation untracking is now verified: Git reports zero files tracked under Documentations. The next functional setup is direct Google Drive backup; follow the updated remaining-work list in Documentations/37_RELEASE_STATUS.md.

Direct Drive setup is now active. The user has not created the OAuth Web application client yet; reuse Google Cloud project lantern-post-ff565 if desired and register https://the-lantern-post.onrender.com/backups/drive/callback. See Documentations/42_GOOGLE_DRIVE_SETUP.md. setup:drive imports the OAuth Web-client JSON (never the FCM service-account file), validates the registered callback, saves all four settings together and preserves any existing token key. check:drive-config is local validation only. Do not add partial live Drive configuration while credentials are missing, print private values, regenerate a valid token key or claim Google/Render configuration and real backup acceptance before they happen. Keep Google OAuth Testing restrictions and token expiry distinct from app-store publication.

Native Google work now supersedes basic Drive setup: the user reports Web OAuth working and wants the device account picker for both sign-in and Drive in an installed Android APK. See Documentations/43_NATIVE_GOOGLE_HANDOFF.md. Native backend endpoints and a pure client coordinator are prepared; native UI integration is unfinished because @clerk/expo-google-signin@1.0.4 and @react-native-google-signin/google-signin@16 are not installed. The user has not run the install command; the agent's download was blocked and approval review failed with 404. Do not fake installed packages or claim native/device verification. Preserve browser PKCE; native grants use a separate authenticated, owner-bound one-use state with domain-separated hashes, cancellation and durable token-cleanup fences. Keep the current Drive Web client/key and Clerk identities. Next work is package installation, SDK adapters/plugin configuration, actual Android signing/client registration and an APK rebuild. The native picker briefly backgrounds the Activity; exempt only that interaction while retaining route/account cancellation and a foreground fence before code exchange.

Native integration update: the user installed @clerk/expo-google-signin 1.0.4 and Google Sign-In 16.1.5. Android sign-in now uses Clerk's native hook; Drive uses the SDK's server-auth-code flow through the prepared owner-bound endpoints. Plugins, lazy native-module checks, age/cancellation handling, exclusive picker state and bounded foreground return are wired. Decrypted archives/keys still clear on background. Browser/iOS/old APK compatibility paths remain; do not call those native picker verification. The public EXPO_PUBLIC_CLERK_GOOGLE_WEB_CLIENT_ID in client/.env reuses the existing Drive Web client as the chosen setup; Clerk's Google connection must match, or the operator can supply its other custom Web client. No secrets or token keys were copied/rotated. check:native-google validates local readiness only. Expo Android prebuild/autolinking and all-platform JS exports pass, but dashboard settings, actual signing SHA-1, a new APK and physical-device tests remain required. See the updated Documentations/43_NATIVE_GOOGLE_HANDOFF.md; do not repeat package installation.

The phone icon now uses the user's supplied painted lantern, sealed envelope and moonlit palace image. Preserve client/artwork-source/launcher-original.png and the separate client/assets/app-icon assets selected by app.json. scripts/render-app-icons.mjs creates the launcher/adaptive/themed/notification/favicon variants locally; the older storybook renderer must not replace them. Native resources were generated for verification, but the new icon still needs an APK rebuild and installation. Keep the existing in-app ornaments and splash mark separate.

The user requested a separate Next.js showcase site for Vercel. It lives in `showcase-website/`, outside the root npm workspaces, with its own lockfile and locally bundled artwork. Vercel Root Directory must be that exact folder. Preserve the interactive sample-only tours, five real Expo-web app captures using synthetic content, accessible screenshot gallery and full guide. All APK links come from `src/config/site.ts`; the supplied RollCall URL is explicitly a temporary link, not a verified Lantern Post release. No private app API, authentication, tracking or paid service belongs in this showcase. `.easignore` excludes it from APK uploads. Production build, lint, types and browser checks pass locally; remote Vercel/GitHub builds have not been claimed as completed. The former folder name contained a space, which caused Vercel to reject its generated Serverless Function name. Preserve the hyphenated folder and update the existing Vercel project's Root Directory when deploying this rename; npm install warnings were not the reported fatal error.

The user now confirms showcase work complete and requested in-app guidance. Preserve the anchored palace tour in client/src/guidance: actual measured section/control targets, gold spotlight and pointer, Next/Back/Finish/Skip, and permanent replay from the home header, Guidance section and account menu. Auto-start once on the first eligible palace visit after onboarding/arrival, using a content-free installation marker in native document storage (browser profile storage on web). Never reset it per account or force it after skip, finish or an interrupted visit. Wait for foreground/home focus and other dialogs; stop on route/identity changes and pause over backgrounding. Guidance suppresses arrival popups without suppressing itself. It must never open or mutate a draft, record, send, publish, burn, or alter the user's motion preference. Keep bounded measurements, responsive safe-area placement and storage-failure retry. Native exports pass but do not imply physical-device install verification. See Documentations/45_PALACE_GUIDANCE_HANDOFF.md.
