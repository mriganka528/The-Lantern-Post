import { Text } from 'react-native';
import { s } from '../storybook/story-ui';
export function VoicePlayer(_props: { uri: string; durationMs: number; label?: string }) { return <Text style={s.body}>Voice playback is unavailable in this build.</Text>; }
